/**
 * 存放对 msui 的 config，需在 zepto 之后 msui 之前加载
 * 
 * Created by bd on 15/12/21.
 */
$.config = {
	router : true
};
// 系统全局变量
$.GlobalVar = {
	serverURL : 'http://localhost:8080/om/',

	imageURL : '/om/image.jsp?path=',
}

